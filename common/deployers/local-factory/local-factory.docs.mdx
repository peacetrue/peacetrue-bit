---
labels: ['module']
description: '本地部署器工厂，用于创建一个本地部署器。'
---

API:

```ts
import type {localFactoryProps} from "./local-factory";
import type {DeployFn} from '@teambit/application';
function localFactory(props: localFactoryProps): DeployFn;
```
